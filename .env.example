# Wuh Water Doing? needs no environment variables and no API keys.
#
# Outage notices come from the Barbados Water Authority public RSS feed,
# fetched and parsed server-side in src/app/api/outages/route.ts.
# The map uses OpenStreetMap tiles (free, no key).
